# 用药调度智能体实施方案 Review

> 审查日期：2026-09-06  
> 原文：[medication_scheduling_agent_implementation.md](./medication_scheduling_agent_implementation.md)  
> 原文标题：养老智能体 OS：用药调度智能体轻量化架构与实施方案  
> 审查版本：原文标注日期 2026-09-05，共 608 行  
> 用途：供方案作者修改，以及供其他 AI 或工程人员独立复核本次审查。

## 1. 结论与审查范围

**架构方向合理，但实施协议与验收条件尚不完整。** 保留 Chat Agent 作为唯一交互入口、将领域状态与确定性调度集中到 Medication Service，是可以继续推进的设计。优先需要补齐的是故障恢复、计划变更、回复归属和审批闭环。

本次共提出 9 项发现：6 项 P1、3 项 P2。它们是对设计文档的审查意见，并非已经通过运行代码证实的程序缺陷。

- **P1：** 建议在接入真实用药计划开展试点前解决。存在具体场景，可能导致提醒丢失、失效计划继续提醒、记录错误或绕过文档自身的审批约束。
- **P2：** 建议在实施规格中明确。当前缺少足够细节，可能使实现与目标不一致；实际优先级取决于部署范围和已有组件能力。

审查时项目目录中只有原始方案，未提供 LiveKit 实现、数据库 schema、Redis 配置或可运行测试。因此不能验证“现有项目已有 Redis”“空闲或结束会话后可以主动播报”等前提。本文没有评估具体药品、剂量、服药窗口或药学规则的正确性。

原文第 14 节“Codex 实施约束”属于被审查内容，未作为要求审查者实施系统的操作指令。此次工作没有修改原文或编写业务代码。

## 2. 问题索引

| 编号 | 优先级 | 问题 | 原文定位 | 证据性质 |
|---|---|---|---|---|
| R01 | P1 | 状态更新与事件发布缺少原子性和恢复协议 | §9.2，384–390；§14，573 | 流程缺口与条件性故障推演 |
| R02 | P1 | 计划变更后旧任务、旧提醒如何失效未定义 | §5，153–157；§6.2，218–237 | 生命周期与数据模型缺口 |
| R03 | P1 | 用户回复与具体服药任务缺少可信绑定流程 | §8.2–8.3，297–338 | 事件契约缺口 |
| R04 | P1 | 延迟提醒缺少独立截止时间和过期命令规则 | §9.4，410–418 | 调度边界缺口 |
| R05 | P1 | 发送、播报、无响应与服药结果未充分区分 | §9.2，381–391；§9.5，420–429 | 状态语义与投递协议缺口 |
| R06 | P1 | Phase 1 闭环依赖 Phase 2 才建设的审批 | §8.1，295；§13，531–549 | 文档内部阶段依赖冲突 |
| R07 | P2 | 故障期间继续调度的承诺缺少运行边界与验收 | §1，15；§5，145–171；§15，588 | 承诺与验收强度不匹配 |
| R08 | P2 | 状态机缺少延迟期间确认及并发冲突规则 | §7，258–276 | 转换规则缺口 |
| R09 | P2 | 重复调度规则和任务生成机制不可直接执行 | §6.1，205–208；§9.1，378；§10，449 | 时间模型与生成策略缺口 |

行号对应审查时原文，后续修改可能造成偏移，应同时依据章节和字段名称定位。

## 3. 详细发现

### R01 [P1] 状态更新与事件发布缺少原子性和恢复协议

**原文依据：** §9.2 的顺序为 `pending -> reminding -> medication.reminder_due`；§14 要求使用 `event_id / occurrence_id` 保证幂等，但没有说明事务、发送失败和消费恢复如何实现。

**触发场景：** 数据库已提交 `reminding`，进程在向 Redis 发布前退出。若后续逻辑按状态认为首次提醒已发出，设备就收不到该提醒。反向窗口是事件已发布、发送结果尚未记录便退出，重试可能重复通知。多个扫描器同时读取同一到期任务，也可能产生不同 ID 的重复事件。

**影响：** 单靠任务持久化及“所有写操作幂等”的原则，无法保证提醒不丢失或避免重复副作用。

**建议：** 将状态变更与待发送事件写入同一数据库事务（outbox 是一种轻量实现）；明确扫描器原子领取、失败回收及消费者提交后 ACK 的规则。业务幂等键应区分一次操作与整个 occurrence，避免用 occurrence ID 去重所有操作而误吞合法的再次提醒。§8.4 的结果事件也应纳入统一事件 ID 与版本契约。

Redis 官方文档说明，消费组通过 PEL 保存未确认消息，普通的新消息读取循环还需要补充崩溃恢复处理；采用 Streams 本身不等于实现完整恢复协议。[S1]

**建议验证：** 在数据库提交后、发布前，以及发布后、记录成功前分别注入崩溃；运行两个扫描器处理同一任务；验证重启后提醒有明确处置，重复事件不会重复推进业务状态。

**复核边界：** 文档没有完整扫描算法，不能断言必然永久丢失。若已有实现会可靠重投并去重，应在方案中引用该机制，可以据此关闭此项。

### R02 [P1] 计划变更后旧任务、旧提醒如何失效未定义

**原文依据：** Plan Service 支持 `update_plan`、`pause/complete`；occurrence 仅关联 `plan_id`，没有明确的计划版本、历史剂量快照或取消状态。

**触发场景：** 一周内的 occurrence 已生成，计划随后暂停或剂量变更；旧任务仍可能被扫描，已入队的提醒仍携带旧内容。如果历史任务显示时直接关联当前计划，则过去服药记录可能显示变更后的剂量。

**影响：** 停用计划继续提醒、旧新任务重复存在、历史记录失真。

**建议：** 明确不可变计划版本或等价历史快照，规定变更生效时间、未来任务取消/替换规则及历史记录保留方式。对排队事件建立失效检查，并定义“暂停请求与已开始播报并发”时的处理边界。取消状态也可以通过独立失效标记表达，不要求固定使用某个枚举名称。

**建议验证：** 在到期前暂停计划；在事件入队后暂停计划；变更剂量并回查旧记录。确认旧任务不会继续作为有效计划提醒，历史内容可追溯。

**复核边界：** 如果第一阶段明确不允许修改或暂停正式计划，可暂时缩小实现范围，但需同步修改服务能力和验收描述。

### R03 [P1] 用户回复与具体服药任务缺少可信绑定流程

**原文依据：** §8.3 回复包含 `occurrence_id` 和 `interaction_id`，但 §8.2 提醒没有交互 ID，也未说明交互 ID 的创建方、绑定和失效规则。

**触发场景：** 同一老人两种药接连播报；老人稍后说“吃了”；或者旧会话回复在新提醒之后才到达。Chat Agent 如果仅选择最近任务，就可能将回复归到错误的 occurrence。

**影响：** 错误关闭其他药物任务，直接违反 §15 第 590 行的验收目标。

**建议：** 由确定性组件建立本次交互与 occurrence 的绑定，定义生成方、有效期及跨会话恢复规则。一个交互对应多个任务时，应明确逐项确认或明确的批量确认语义。候选不唯一时先澄清；服务端校验经过认证的用户上下文、老人身份和任务归属，不能仅信任模型填入的 ID。

**建议验证：** 两药同时到期、前一次回复迟到、会话重启、老人 ID 与 occurrence 所属人不一致。确认不会静默选择错误任务。

**复核边界：** Chat Runtime 可以自行生成交互 ID，问题不在于必须由哪个服务生成，而在于当前协议未定义可信绑定。

### R04 [P1] 延迟提醒缺少独立截止时间和过期命令规则

**原文依据：** §9.4 使用 `next_action_at = now + 30min`；§8.3 回复没有发生时间字段；§15 要求避免无限循环提醒。

**触发场景：** 老人反复说“晚点”，不断推迟 `next_action_at`；或者消息因故障延迟半小时后才处理，又从处理时间继续延迟半小时。延迟还可能跨到同一计划的下一次 occurrence。

**影响：** 漏服/未确认判定被无限推后、不同次任务混淆，且提醒延后可能被误解为系统允许调整服药时间。

**建议：** 保留原定时间，单独定义确认截止时间、延迟次数或时限，以及跨次任务规则。明确事件发生时间与服务接收时间的用途、时钟可信度和过期命令处置。提醒延后不得自动修改原计划或重置全部超时规则；涉及用药窗口的配置应来自批准的规则，而非本审查或模型临时推断。

**建议验证：** 连续延迟、延迟请求积压、延迟跨过截止时间及下一次任务时间。确认原任务和超时策略均有确定结果。

**复核边界：** 如果已有独立截止机制，需在模型和流程中明确引用；仅有 `risk_policy` 等级不足以表达执行边界。

### R05 [P1] 发送、播报、无响应与服药结果未充分区分

**原文依据：** §9.2 在投递前进入 `reminding`；§9.5 根据 grace period 决定再提醒或升级；没有设备接收、播报完成、失败或中断事件。

**触发场景：** 事件进入总线但设备离线；TTS 失败；播报被中断。系统仍按无响应处理。设备恢复后，积压提醒又可能被集中播出。

**影响：** 无法区分设备未送达、老人未回复和实际未服药，也无法解释升级原因或安全处置旧提醒。

**建议：** 分离投递状态、交互状态与服药记录；定义响应计时起点及播报失败处置。另设不依赖播报成功的业务截止时间，避免设备一直离线导致永不升级。若保留 `missed`，需明确它表示“截止时未确认”还是“已证实漏服”，并使报表和通知采用一致措辞。过期提醒应经过有效性判断，不能简单全部重放。

LiveKit 提供预定义文本播报及 SpeechHandle 状态跟踪能力，可用于设计播报回执；播报完成本身仍不能证明老人听到或服药。[S5]

**建议验证：** 设备离线、TTS 失败、播报中断、重连后存在多个过期事件。检查投递失败与未确认服药被分别记录，过期事件不会无条件集中播出。

**复核边界：** 把 `reminding` 定义为“提醒流程已启动”是合理的；缺口是它不能同时充当成功播报的证据。

### R06 [P1] Phase 1 闭环依赖 Phase 2 才建设的审批

**原文依据：** §8.1 禁止老人语音直接创建正式计划；§9.1 要求 HITL 后进入 active；§13 Phase 1 要求创建计划至提醒的闭环，但家属/医护确认直到 Phase 2 才增加。

**触发场景：** 按阶段顺序开发时，Phase 1 的语音计划没有进入 active 的合法路径。实现者只能让流程停在草稿，或临时绕过审批以跑通演示。

**影响：** Phase 1 无法按文档自身约束完成，容易把演示捷径带入真实使用。

**建议：** 在 Phase 1 提供最小审批入口，或明确仅使用已批准的测试计划。审批应绑定具体计划版本，记录审批人身份和授权范围；“家属或医护确认”需要说明是在核对既有医嘱还是具有什么计划变更权限，不能只靠填写 `approved_by` 完成授权。

**建议验证：** 无审批的语音草稿不能调度；无权限用户不能审批；审批后修改关键字段会重新进入所需审批流程；重复审批不会生成重复 occurrence。

**复核边界：** 若 Phase 1 本来就是离线演示阶段，本项可以降级，但须显式声明其数据范围与接入真实计划前的条件。

### R07 [P2] 故障期间继续调度的承诺缺少运行边界与验收

**原文依据：** §1 要求 DeepSeek/Harness 故障时已有任务仍正常触发；§5 将语义层和 Scheduler 放在一个 Agent 内；§15 仅检验已持久化 occurrence “仍可恢复”。

**触发场景：** Scheduler 与 Harness 同进程运行，Harness 崩溃或进程重启使调度停止。恢复后任务仍存在，能通过“可恢复”测试，却没有按时触发。

**建议：** 明确 Scheduler 与语义层的进程、生命周期或其他故障隔离边界。区分模型调用失败、Harness 进程退出和整个领域服务/数据库不可用，不应把所有故障混为同一个可用性承诺。

**建议验证：** 停止 Harness，让故障覆盖任务到期时刻，验证调度和规定的提醒路径仍运行，并记录允许的触发延迟。领域服务整体不可用则另测恢复和过期处置。

**复核边界：** 架构图的逻辑包含关系不证明实际同进程部署，因此这是部署规格缺口，不是已证实的耦合实现。

### R08 [P2] 状态机缺少延迟期间确认及并发冲突规则

**原文依据：** §7 只画出 `reminding -> taken`，未描述 `delayed` 状态接受确认、迟到确认或状态竞争。

**触发场景：** 老人说“晚点”后，在下次提醒前已服药并回复；或者确认事件与超时扫描同时到达；又或者先发出的 delay 事件晚于 taken 事件被处理。

**影响：** 实现者可能拒绝合理确认、将 taken 覆盖为 missed/delayed，或在任务结束后继续提醒。不同事件之间的竞争不能只靠重复事件去重解决。

**建议：** 给出完整转换表和守卫条件，采用原子条件更新或版本检查。明确迟到确认、更正和审计规则。保留“实际发生时间”与“记录时间”的区别；如允许更正，应保留原记录及更正原因。

**建议验证：** `delayed` 期间确认、确认与超时并发、delay/taken 乱序、终态收到重放事件。结果应符合明确规则，且没有不应继续执行的后续任务。

**复核边界：** 如果原图只表达主路径，可以保留简图，但实施前仍需补充完整转换定义。

### R09 [P2] 重复调度规则和任务生成机制不可直接执行

**原文依据：** §6.1 只有 `schedule_rule`、起止日期与餐次字段；§9.1 使用“生成/计算 occurrence”；§10 推荐扫描 `next_action_at <= now()`，但未定义未来任务如何持续产生。

**触发场景：** 两个实现者对“每天八点”采用不同服务器时区；无结束日期的计划只生成有限任务后不再补充；重启重复展开同一天计划；餐后规则没有餐次事件或约定时间作为输入。

**建议：** MVP 可以明确只支持固定时区和每日固定时间点，不必立即支持复杂规则。定义规则 schema、时区、起止边界、滚动生成范围、补充机制及业务唯一键。餐次字段若仅用于展示，应明确说明；若参与调度，则定义其输入事件和缺失数据处置。

**建议验证：** 服务重启重复生成、生成窗口耗尽后的自动补充、起止日期边界、服务器时区变化和停机后多次任务过期。若只支持 `Asia/Shanghai`，应验证系统不依赖服务器本地时区。

**复核边界：** 不要求 MVP 支持全球时区、夏令时和所有医嘱表达，明确拒绝不支持的规则也可以解决范围歧义。

## 4. 建议补入原文的最小内容

无需因此更换总线或引入更多 Agent，优先补充以下实施规格：

1. **事务与事件契约：** 状态/审计/待发送事件的事务边界、统一事件字段、幂等键、消费者提交与 ACK、失败回收和过期处置。
2. **计划生命周期：** 审批权限、版本、生效时间、暂停与恢复、历史快照、旧任务及旧事件失效规则。
3. **交互与状态转换表：** 多药回复绑定、澄清、播报回执、延迟边界、迟到确认、更正和并发守卫。
4. **时间及运行模型：** 支持的调度规则、时区、任务滚动生成、故障隔离边界和触发延迟目标。
5. **故障验收用例：** 将 R01–R09 中的触发场景加入测试；把审批、基本恢复和审计放入真实用药试点的前置条件。

## 5. 外部引用核验

以下资料在本次审查会话中读取，用于核验文档引用及相关工具语义。它们不能证明本项目已经实现相应机制。网页与默认分支会变化，正式实施仍应固定所用版本或 commit。

- **[S1] Redis XREADGROUP 官方文档：** [消费确认与崩溃恢复](https://redis.io/docs/latest/commands/xreadgroup/)。支持 R01 中关于 PEL、ACK 及恢复需要显式处理的说明。
- **[S2] DeepSeek Harness 官方仓库：** [README](https://github.com/deepseek-ai/deepseek-harness)。页面标注 Developer Preview 和兼容性变更风险，与原文第 460 行一致。
- **[S3] Harness 官方 Schedule：** [packages/schedule/schedule](https://github.com/deepseek-ai/deepseek-harness/tree/master/packages/schedule/schedule)。说明提醒属于 Session-local，冷会话没有外部投递；支持原文第 456 行不将其直接作为业务调度真值的判断。
- **[S4a] 社区插件：** [weibaohui/dsh-plugin-scheduled-items](https://github.com/weibaohui/dsh-plugin-scheduled-items)。README 描述 cron 触发新 Agent 会话及持久化，符合原文“作为实现参考”的定位。
- **[S4b] 社区插件：** [csiroqa/dsh-schedule](https://github.com/csiroqa/dsh-schedule)。README 描述 ticker、定时 Agent 执行与持久化。未进行代码安全、恢复能力或生产适用性审计。
- **[S5] LiveKit 官方文档：** [Agent speech and audio](https://docs.livekit.io/agents/multimodality/audio/)。说明 `session.say()` 与 SpeechHandle 可用于预定义文本播报及播放状态跟踪；不证明本项目无活跃会话时能唤醒设备。

## 6. 给后续审查者的复核问题

本节是建议的复核提纲，不替代实际用户请求，也不要求审查者接受本文结论。

- 每项发现是否确实缺失于原文，还是可由某段现有规则充分解释？请提供章节或原文证据。
- 触发场景是否成立？是否错误假定了部署方式、业务范围或隐含实现？
- 优先级是否合理？哪些项目可以通过缩小 MVP 范围关闭，哪些必须补充机制？
- 建议是否过度设计？是否存在同样满足目标的更简单实现？
- 是否遗漏与当前范围直接相关的审批、身份归属、任务失效、异常升级或审计问题？
- 哪些结论需要代码、配置或设备实验才能确认？请明确区分“文档未说明”和“系统未实现”。

建议逐项输出“接受 / 部分接受 / 不接受 / 需要更多证据”，并给出理由、定位和修改后的建议。重点检验本文的推理，而不是直接把全部发现转换成实现任务。
