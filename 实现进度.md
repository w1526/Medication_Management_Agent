# 用药提醒智能体 MVP 实现进度

更新时间：2026-09-12  
对应方案：[medication_reminder_mvp_implementation_v2.1.md](./medication_reminder_mvp_implementation_v2.1.md)

## 1. 当前结论

当前版本已经完成一个可运行、可持久化、可测试的“用药提醒领域服务 MVP”，并增加了一个用于演示和测试的 Web 界面。

领域状态链路和本地 Web 用户提醒链路已经打通，但真实设备/语音提醒链路尚未打通：

```text
创建计划草稿
  → 审批生效
  → 生成未来 7 天 MedicationOccurrence
  → Scheduler 到点扫描
  → Reminder Event / Outbox
  → 本地 Device Adapter 记录 dispatched（模拟）
  → Web 轮询提醒 + 页面操作（MVP 已实现）
  → TTS / LiveKit 真实播报（尚未接入）
  → 手动模拟老人回复：吃了 / 晚点 / 跳过
  → 更新服药状态
  → 查询今日状态与审计事件
```

但需要明确：

> DeepSeek Harness 语义适配器和自然语言 Agent API 已接入；已使用项目 `.env` 中的 Key，通过 `deepseek-v4-flash` 完成真实计划抽取验证。LiveKit、Redis Streams 和真实 TTS 仍未接入。

前次实测（2026-09-11）发现：E001 的计划在 Asia/Shanghai `18:02` 正常生成 `medication.reminder_due` 和 `device.interaction.request`，Outbox 状态为 `published`，提醒尝试状态为 `dispatched`；但没有 `started/completed` 的设备回执，任务在 `20:02` 因无人确认变为 `closed_unconfirmed`。该问题已通过 Web 轮询提醒通道补齐本地测试闭环；真实设备播报和语音交互链路仍未实现。

## 2. 已实现内容

### 2.1 计划生命周期

- 创建 `Plan Draft`。
- 草稿未经审批不会进入调度。
- 支持提交到 `pending_confirmation`。
- 支持审批进入 `active`。
- 支持暂停计划。
- 支持创建新计划版本。
- 关键字段修改不会覆盖历史版本。
- 计划暂停或新版本生效后，未来旧任务会标记为 `cancelled`。
- 计划和 occurrence 保存药品、剂量、餐前/餐后等快照。

对应代码：

- [service.py](./src/medication_reminder/service.py)
- [storage.py](./src/medication_reminder/storage.py)

### 2.2 调度与 Occurrence

- 只支持 `Asia/Shanghai`。
- 只支持每日固定时间 `HH:MM`。
- 支持起始日期和可选结束日期。
- 审批后自动生成未来 7 天 occurrence。
- 每次具体服药任务保存：
  - `scheduled_at`
  - `confirmation_deadline_at`
  - `next_reminder_at`
  - `plan_id`
  - `plan_version`
  - 药品和剂量快照
- Scheduler 每秒运行一次。
- 同一 occurrence 的提醒领取使用条件更新，避免多个 Scheduler 重复领取。
- 服务重启后会重新检查 active plan 并补齐未来任务。

### 2.3 服药状态机

当前状态包括：

```text
unconfirmed
  ├─ CONFIRM_TAKEN → confirmed_taken
  ├─ SKIP          → skipped
  ├─ deadline 到期 → closed_unconfirmed
  └─ DELAY         → 仍为 unconfirmed，修改 next_reminder_at

计划暂停/版本替换 → cancelled
```

已经实现：

- “吃了”确认。
- “跳过”记录。
- “晚点”延迟提醒。
- 延迟次数限制。
- 延迟不能越过确认截止时间。
- `scheduled_at` 不会因延迟而修改。
- 未确认不会自动变成已服用。
- 已完成状态不能被重复确认覆盖。

### 2.4 Interaction 绑定

- 每次提醒创建独立 `medication_interaction`。
- interaction 与一个 occurrence 绑定。
- 老人响应必须携带可信 `interaction_id`。
- 服务端校验：
  - interaction 是否存在；
  - elder 是否匹配；
  - occurrence 是否匹配；
  - interaction 是否过期；
  - occurrence 是否仍处于 `unconfirmed`。
- 不允许依靠 LLM 自己猜 occurrence ID。

### 2.5 提醒投递状态

已经区分提醒请求和服药事实：

```text
queued
  → dispatched
  → started
  → completed

失败分支：failed / interrupted / expired
```

当前可以通过 `POST /api/v1/medication/device-events` 模拟设备回执。

设备播报完成不会自动把任务记为 `confirmed_taken`。

当前本地 Device Adapter 只更新 `reminder_attempt.delivery_status=dispatched`，不会播放 TTS、调用 LiveKit 或向浏览器推送提醒；Outbox 显示 `published` 只代表本地模拟发布器已消费事件，不代表老人已经听到提醒。

### 2.6 事件、Outbox 和审计

- 所有领域状态变化会写入 `medication_event_log`。
- 状态更新、审计事件、待发布 Outbox 事件在同一个 SQLite 事务中提交。
- `domain_outbox` 保存待发布事件。
- Outbox 发布失败会恢复为 pending 并记录错误。
- 通过 event ID 和业务 dedup key 实现幂等。
- 支持重复用户响应不重复推进状态。
- 支持进程重启后重新处理 pending outbox。
- 计划暂停/版本失效后，过期提醒不会继续生成设备请求。

### 2.7 HTTP API

当前已经提供：

```text
POST /api/v1/medication/plans/draft
POST /api/v1/medication/plans/{id}/submit
POST /api/v1/medication/plans/{id}/approve
POST /api/v1/medication/plans/{id}/pause
POST /api/v1/medication/plans/{id}/revise

GET  /api/v1/medication/plans
GET  /api/v1/medication/plans/{id}
GET  /api/v1/medication/today
GET  /api/v1/medication/notifications?elder_id=E001
GET  /api/v1/medication/occurrences/{id}
GET  /api/v1/medication/agent/status

POST /api/v1/medication/responses
POST /api/v1/medication/agent/message
POST /api/v1/medication/device-events
POST /api/v1/medication/scheduler/run

GET  /api/v1/medication/events
GET  /api/v1/medication/events/outbox

GET  /health/live
GET  /health/ready
```

### 2.8 Web 测试界面

已经增加基于 Tabler Core Dashboard 风格的测试台：

- 创建草稿。
- 审批计划。
- 暂停 active 计划。
- 手动运行一轮 Scheduler。
- 查看今日任务和状态统计。
- 点击“吃了 / 晚点 / 跳过”。
- 查看计划版本。
- 查看最近审计事件。
- 查看当前服务在线状态。
- 后台 Scheduler 产生新提醒后，页面每 2 秒轮询当前老人的打开提醒并显示提醒卡片；支持在提醒卡片上直接点击“吃了 / 晚点 / 跳过”。
- 网页端增加标准流程指示器，并在自然语言结果中展示解析后的药品、剂量、24 小时制时间、开始日期和下一步审批按钮。

相关文件：

- [index.html](./web/index.html)
- [app.css](./web/assets/app.css)
- [app.js](./web/assets/app.js)

访问地址：

```text
http://127.0.0.1:18080/
```

## 3. 已验证内容

当前测试文件：[test_mvp.py](./tests/test_mvp.py) 与 [test_semantic_agent.py](./tests/test_semantic_agent.py)

已通过 15 个单元测试：

1. 草稿审批和 occurrence 生成。
2. Scheduler 单次领取与设备请求生成。
3. 用户确认幂等。
4. 延迟不修改原定时间。
5. 截止时间关闭未确认任务，暂停取消未来任务。
6. 计划版本修改和旧版本快照保留。
7. 两个 Scheduler 并发运行不重复领取。
8. 服务重启后 Outbox 恢复。
9. 自然语言计划只创建 Draft。
10. 已打开 interaction 的自然语言回复绑定到正确 occurrence。
11. 无提醒上下文的普通文本不会误调用计划解析。
12. 多个打开提醒时必须提供可信 interaction_id，避免误绑定。
13. Web 提醒接口显示 active interaction，确认后从提醒列表清除。
14. 自然语言缺少开始日期时默认从 Asia/Shanghai 当天开始。
15. 中文“晚上10点”在确定性边界统一归一化为 24 小时制 `22:00`。

另外已验证：

- Python 源码可编译。
- HTTP 服务可以启动。
- `/health/live` 返回 200。
- `/` 返回 Web 页面。
- `/assets/app.js` 返回静态资源。
- 实际运行中的 `18080` 服务健康检查返回 200。
- 实际数据库已验证 Scheduler 在计划时间生成 reminder 和 device request，但设备尝试停留在 `dispatched`，没有真实播报回执。
- 任务最终因没有用户确认进入 `closed_unconfirmed`，这与“没有按时提醒”的现象一致。
- Web 提醒通道已增加：页面可读取未过期 interaction，并将其显示为用户可操作的提醒卡片。

运行测试：

```bash
PYTHONPATH=src .venv/bin/python -m unittest discover -s tests -v
```

## 4. 当前仍未实现的内容

### 4.1 DeepSeek Harness 语义层（已接入，已完成首次真实调用）

已经实现：

- `HarnessSemanticAdapter`，通过官方 `deepseek-harness-sdk` 调用 DeepSeek Harness。
- `POST /api/v1/medication/agent/message` 自然语言入口。
- `GET /api/v1/medication/agent/status` 运行状态检查。
- 自然语言计划抽取为结构化 Draft。
- 自然语言未提供开始日期时，确定性业务层默认使用 `Asia/Shanghai` 当天。
- 中文“晚上/下午”等时段会在确定性边界转换为 24 小时制，避免模型将“晚上10点”落成 `10:00`。
- 自然语言回复抽取为 `CONFIRM_TAKEN / DELAY / SKIP / REPEAT`。
- Harness 不接触 Medication DB，不直接审批、调度或调用设备。
- Harness 结果必须先解析为 JSON，再交给确定性业务服务校验。
- 没有 API Key 或 SDK 时返回明确配置错误，不影响原有调度链路。
- Web 测试台已增加自然语言 Agent 输入区。

当前仍需验证：

- 使用真实 `deepseek-v4-flash` 验证 `CONFIRM_TAKEN / DELAY / SKIP / REPEAT` 响应分类。
- 验证不同模型返回格式、超时、限流和错误重试。
- 增加自然语言歧义澄清交互。
- 评估 Harness session 日志中的医疗文本留存和脱敏策略。
- 接入真实 Chat Agent 的 ASR 文本，而不是只从测试页面发送文本。

当前本地 Fast Path 仍优先处理“吃了 / 晚点 / 跳过 / 再说一遍”，模型不可用时确定性调度不受影响。

### 4.2 LiveKit / Chat Agent 接入

尚未实现：

- LiveKit Chat Agent 接收 `device.interaction.request`。
- 无活跃聊天时的主动播报。
- Chat Agent 将 ASR 文本转为 `medication.user_response`。
- 当前 interaction 上下文注入对话运行时。
- TTS 开始、完成、失败、打断回执。
- `elder_id -> deviceSn -> LiveKit` 的真实设备路由。

当前 Web 页面已提供本地轮询提醒通道；Chat Agent、真实设备适配器和 LiveKit 仍处于模拟/未接入状态。

### 4.3 Redis Streams / 生产事件总线

当前 Outbox 是 SQLite 内的本地发布器，不是真正的 Redis Streams Consumer Group。

尚未实现：

- Redis Streams。
- Consumer Group。
- PEL pending message reclaim。
- 消费确认和失败重试策略。
- 多进程 Outbox Worker。
- 跨服务事件认证和版本兼容。

### 4.4 PostgreSQL / 生产存储

当前使用 SQLite，适合本地 MVP 和单进程测试。

尚未实现：

- PostgreSQL schema/migration。
- 连接池。
- 多实例并发部署验证。
- 数据备份和恢复策略。
- 数据保留与脱敏策略。

### 4.5 用户、权限和安全

当前 API 没有认证授权，测试界面也没有登录。

尚未实现：

- 家属/医护/管理员角色。
- 审批权限校验。
- elder 数据访问隔离。
- API Token 或 JWT。
- HTTPS、CORS、CSRF 等部署安全配置。
- 敏感医疗数据访问审计。

### 4.6 家属可见能力增强

当前只有今日查询和基础事件查看。

尚未实现：

- 家属独立账号和页面。
- 历史服药趋势。
- 未确认任务筛选。
- 家属通知。
- L3/L4 异常升级。
- 邮件、短信、企业微信或 App 推送。

### 4.7 方案明确排除的后续能力

以下内容不是当前 MVP 缺陷，而是 V2.1 明确放到后续阶段：

- 药物相互作用检查（M2 / DDI）。
- DrugBank / OpenFDA。
- FHIR / EHR 同步。
- 智能药盒。
- 动态餐时调度。
- PRN 按需用药。
- 隔日和复杂周期规则。
- M7 Reflection Sub-agent。
- 长期语义记忆和向量数据库。
- Prompt Evolution / 自进化。
- 自动调整服药时间或剂量。

## 5. 与 V2.1 验收清单对照

| 验收项 | 状态 | 说明 |
|---|---|---|
| 创建 Plan Draft | 已实现 | API 和 Web 页面均可创建 |
| 未审批计划不调度 | 已实现 | 只有 active plan 生成/领取任务 |
| 审批后生成 occurrence | 已实现 | 默认生成未来 7 天 |
| 到点提醒事件 | 已实现 | 本地 Scheduler + Outbox |
| 真实设备播报 | 未实现 | 当前为本地 Device Adapter 模拟，只记录 dispatched |
| 浏览器主动提醒 | 已实现（MVP） | 页面每 2 秒轮询未过期 interaction，并提供操作按钮 |
| 吃了 | 已实现 | interaction 校验 + 幂等 |
| 晚点 | 已实现 | 只修改 next_reminder_at |
| 跳过 | 已实现 | 记录 skipped |
| 截止时间关闭未确认 | 已实现 | closed_unconfirmed |
| 今日状态查询 | 已实现 | API 和 Web 页面 |
| 审计 Event Log | 已实现 | SQLite 持久化 |
| 服务重启恢复 | 已实现 | occurrence + outbox 恢复 |
| 重复 Event 幂等 | 已实现 | event_id / dedup key |
| 计划暂停后旧事件失效 | 已实现 | 消费时再次校验计划有效性 |
| 计划版本与历史快照 | 已实现 | 旧版本保留，未来任务失效 |
| Redis Consumer Group 恢复 | 未实现 | 还没有接 Redis Streams |
| Chat Agent / LiveKit 协同 | 未实现 | 当前只有 API 和模拟界面 |
| DeepSeek Harness 语义解析 | 部分实现 | Adapter/API/真实计划抽取/离线测试已完成，响应分类与异常策略仍需验证 |
| 家属权限和通知 | 未实现 | 当前是内部测试界面 |

## 6. 推荐后续实现顺序

### Phase A：接入语义层

1. 用真实 `deepseek-v4-flash` 请求继续验证响应分类和异常策略。
2. 完善低置信度、超时、限流和歧义澄清。
3. 接入 Chat Agent 的 ASR 文本，继续由可信运行时提供 interaction ID。
4. 做 Harness session 日志的敏感数据留存评估。

### Phase B：接入 Chat Agent 和设备

1. 定义 Chat Agent ↔ Medication Service 的事件契约。
2. 用 Redis Streams 替换本地 Outbox Publisher。
3. 增加 Device Event Adapter。
4. 接入 LiveKit 主动播报。
5. 接收播报生命周期回执。
6. 做“无活跃聊天时主动播报”和设备离线实验。

### Phase C：生产化

1. SQLite 迁移到 PostgreSQL。
2. 增加认证、审批授权和数据隔离。
3. 增加结构化日志、指标、告警、备份和恢复演练。
4. 增加多实例并发和故障注入测试。
5. 增加家属查询、历史趋势和通知能力。

### Phase D：医疗能力扩展

在前面三阶段稳定后，再实现 DDI、FHIR、智能药盒和长期反思能力。

## 7. 当前启动方式

```bash
PYTHONPATH=src .venv/bin/python -m medication_reminder \
  --db data/medication.db \
  --port 18080
```

启动成功后，终端保持运行是正常现象。浏览器访问：

```text
http://127.0.0.1:18080/
```

停止服务：

```text
Ctrl+C
```

