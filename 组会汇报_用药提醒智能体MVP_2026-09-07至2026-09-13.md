# 用药提醒智能体 MVP：上周工作汇报

> 汇报周期：2026-09-07 ～ 2026-09-13  
> 当前阶段：可运行、可持久化、可测试的 MVP  
> 项目目录：`Medication_Management_Agent`

## 一句话结论

上周完成了用药提醒领域服务的本地闭环：用药计划经过审批后生成具体服药任务，Scheduler 到点触发提醒，用户可以确认“吃了”、延迟或跳过，系统会更新任务状态并留下审计记录。同时接入了 DeepSeek Harness 自然语言入口和 Web 测试台。

当前闭环已经能够在本地演示和自动化验证，但真实的 LiveKit/TTS 设备播报、Redis Streams、PostgreSQL 以及用户权限体系还没有接入。

## 1. 本周目标与产出

| 目标 | 产出 | 状态 |
|---|---|---|
| 建立确定性的用药领域服务 | 计划、任务、提醒、服药状态和审计事件均由服务端维护 | 已完成 |
| 打通提醒到服药结果的 MVP 闭环 | Draft → 审批 → Occurrence → Scheduler → Reminder → 用户响应 | 已完成 |
| 接入自然语言能力 | DeepSeek Harness 解析药品、剂量、时间和响应意图 | 已接入，仍需扩大验证 |
| 提供可视化演示入口 | Web 测试台支持计划创建、审批、调度和响应 | 已完成 |
| 验证可靠性边界 | 幂等、并发领取、重启恢复、版本失效等测试 | 已完成 |

## 2. 当前系统架构

```text
用户 / 家属 / Web 测试台 / Chat Agent
                    │
                    ▼
        MedicationSemanticAgent
     （自然语言解析；不拥有业务状态）
          │                  │
          │                  └─ DeepSeek Harness
          ▼                     （受限 JSON 输出）
       MedicationService
  （计划、调度、状态机、幂等、审计）
          │
          ├─ SQLite：计划 / 任务 / interaction / event log / outbox
          ├─ Scheduler：每秒扫描到期任务
          ├─ 本地 Device Adapter：模拟发布并记录 dispatched
          └─ Web notifications：轮询打开的提醒并提供操作按钮
```

核心设计边界：

- LLM 只负责从自然语言中提取结构化意图，不直接修改数据库、不审批计划、不决定任务状态。
- 到点调度由确定性 Scheduler 触发，不依赖 Harness 是否在线。
- 老人响应必须绑定可信的 `interaction_id`，不能让语义模型自行猜测 `occurrence_id`。
- 提醒投递事实与服药事实分开记录：设备显示 `dispatched` 不等于老人已经服药。

## 3. 上周完成的主要工作

### 3.1 用药计划生命周期

- 支持创建 `draft` 计划。
- 草稿必须经过 `submit` 和审批，进入 `active` 后才参与调度。
- 审批后自动生成未来 7 天的 `MedicationOccurrence`。
- 支持暂停计划；暂停后未来任务会变为 `cancelled`。
- 支持创建新计划版本，保留旧版本和历史快照；版本替换后未来旧任务失效。
- 计划和具体任务保存药品、剂量、餐前/餐后等快照，避免后续修改影响历史记录。

### 3.2 Scheduler 与服药状态机

目前支持 `Asia/Shanghai` 时区和每日固定 `HH:MM` 时间。

```text
unconfirmed
  ├─ CONFIRM_TAKEN → confirmed_taken
  ├─ SKIP          → skipped
  ├─ DELAY         → 仍为 unconfirmed，更新 next_reminder_at
  └─ 截止时间到期  → closed_unconfirmed

计划暂停 / 新版本替换 → cancelled
```

每个 occurrence 独立保存：

- `scheduled_at`：原定服药时间，延迟时不修改；
- `confirmation_deadline_at`：允许确认的截止时间；
- `next_reminder_at`：下一次提醒时间；
- `snooze_count`：已延迟次数；
- `plan_id`、`plan_version` 及药品剂量快照。

### 3.3 Interaction 绑定与幂等

- 每次提醒生成独立的 `medication_interaction`。
- 服务端校验 interaction 是否存在、是否属于当前老人、是否过期，以及对应 occurrence 是否仍可处理。
- 同一个老人存在多个打开提醒时，必须由 Chat Agent 显式提供 `interaction_id`，系统返回澄清，不自动猜测。
- 重复响应通过 `event_id` 等机制幂等处理，不会重复推进服药状态或重复写入状态变更事件。

### 3.4 Event Log、Outbox 与恢复

- 领域状态变更写入 `medication_event_log`。
- 状态更新、审计事件和待发布 Outbox 事件在同一个 SQLite 事务中提交。
- `domain_outbox` 保存待发布事件；发布失败可以保留为 `pending` 并重试。
- 服务重启后能够继续处理未发布 Outbox 和未完成的 occurrence。
- Scheduler 使用条件更新领取提醒，两个并发 Scheduler 不会重复领取同一个到期任务。

### 3.5 DeepSeek Harness 语义入口

新增 `HarnessSemanticAdapter` 和自然语言 Agent API：

- `POST /api/v1/medication/agent/message`
- `GET /api/v1/medication/agent/status`

当前支持：

- 从中文描述中提取药名、剂量、每日提醒时间、餐前/餐后和开始日期；
- 计划文本只生成 `draft`，不会由 Harness 直接审批或激活；
- 未提供开始日期时，确定性业务层默认使用 `Asia/Shanghai` 当天；
- “晚上 10 点”等时段在业务边界统一归一化为 `22:00`；
- 对“吃了 / 晚点 / 跳过 / 再说一遍”等响应进行分类；
- Harness 不接触 Medication DB，也不输出或猜测 `occurrence_id`、`interaction_id`；
- 模型不可用时，本地 Fast Path 和确定性调度仍可继续工作。

现有进度记录显示，已使用 `deepseek-v4-flash` 完成真实的计划抽取验证；响应分类、超时/限流/错误重试和歧义澄清仍需继续验证。

### 3.6 Web 测试台

基于 Tabler Core Dashboard 风格完成本地演示界面，支持：

- 自然语言创建计划草稿；
- 审批、暂停和查看计划版本；
- 手动运行一轮 Scheduler；
- 查看今日任务及状态统计；
- 查看当前打开提醒；
- 点击“吃了 / 晚点 / 跳过”；
- 查看最近审计事件和服务健康状态。

页面每 2 秒轮询当前老人的打开提醒，补齐了没有真实设备时的本地测试闭环。

## 4. 可演示流程

### 4.1 启动服务

```bash
cd /root/autodl-tmp/Medication_Management_Agent
PYTHONPATH=src .venv/bin/python -m medication_reminder \
  --db data/medication.db \
  --port 18080
```

浏览器访问：<http://127.0.0.1:18080/>

### 4.2 标准演示步骤

1. 输入：`每天晚上10点提醒我吃氨氯地平5mg`。
2. 查看系统生成的计划草稿，确认时间显示为 `22:00`。
3. 点击“去审批”并审批生效。
4. 到点后等待 Scheduler 生成提醒，或点击“运行一轮调度”检查当前时间。
5. 在提醒卡片中点击“吃了”。
6. 查看今日任务状态、已确认数量和最近审计事件。

也可以直接调用：

```bash
curl -X POST http://127.0.0.1:18080/api/v1/medication/agent/message \
  -H 'Content-Type: application/json' \
  -d '{"elder_id":"E001","text":"每天晚上九点提醒我吃二甲双胍500mg","source":"chat_agent"}'
```

注意：自然语言接口只生成草稿，必须审批后才会生成可调度任务。

## 5. 验证结果

### 5.1 自动化测试

2026-09-14 对当前代码重新执行：

```text
Ran 15 tests in 0.103s
OK
```

覆盖内容包括：

- 草稿审批与 occurrence 生成；
- Scheduler 单次领取和设备请求生成；
- 确认幂等、延迟和截止时间关闭；
- 计划暂停、版本修改和旧版本快照；
- 两个 Scheduler 并发运行不重复领取；
- 服务重启后 Outbox 恢复；
- Web 提醒查询及确认后清除；
- 自然语言计划只创建 Draft；
- interaction 绑定、多提醒澄清和普通闲聊保护；
- 缺少开始日期默认当天；
- 中文“晚上 10 点”统一转换为 `22:00`。

同时完成 Python 源码编译检查，未发现编译错误。

### 5.2 运行场景验证

已有运行记录验证了以下链路：

```text
计划时间到达
  → medication.reminder_due
  → device.interaction.request
  → Outbox = published
  → 本地提醒尝试 = dispatched
```

在 E001 的实测中，设备尝试没有收到 `started/completed` 回执，任务在确认截止时间后进入 `closed_unconfirmed`。这说明当前系统能够正确区分“提醒请求已发出”和“老人已经服药”，但真实设备播报与语音响应链路仍待接入。

## 6. 当前完成度与未完成项

| 模块 | 当前状态 | 说明 |
|---|---|---|
| 计划 Draft / 审批 / 暂停 / 版本 | 已完成 MVP | SQLite 持久化，保留历史快照 |
| 7 天 occurrence 展开 | 已完成 MVP | 每日固定时间，Asia/Shanghai |
| Scheduler | 已完成 MVP | 每秒扫描，支持恢复和并发防重 |
| 服药状态机 | 已完成 MVP | 吃了、晚点、跳过、截止关闭 |
| Event Log / Outbox | 已完成 MVP | 本地 SQLite 事务和幂等 |
| Web 测试台 | 已完成 MVP | 轮询提醒并支持交互 |
| DeepSeek Harness | 部分完成 | 计划抽取已验证，响应与异常策略需扩大验证 |
| 真实 TTS / LiveKit | 未完成 | 当前仅本地 Device Adapter 模拟 |
| Redis Streams | 未完成 | 当前不是 Consumer Group |
| PostgreSQL | 未完成 | 当前使用 SQLite，适合本地 MVP |
| 认证、审批授权、数据隔离 | 未完成 | 当前为内部测试接口 |
| 家属通知、异常升级、历史趋势 | 未完成 | 属于后续产品能力 |
| DDI / FHIR / 智能药盒 | 未开始 | V2.1 明确放到后续阶段 |

## 7. 关键问题与风险

1. **真实提醒链路尚未闭环**：本地 Adapter 只记录 `dispatched`，没有 TTS、LiveKit、设备路由和播报回执。
2. **生产事件总线尚未接入**：SQLite Outbox 适合单进程 MVP，尚不具备 Redis Streams Consumer Group 的多实例恢复能力。
3. **自然语言输入仍需更强防护**：需要继续验证低置信度、歧义、超时、限流、模型返回格式异常和敏感日志留存。
4. **权限与隐私能力不足**：当前没有登录、角色权限、老人数据隔离、HTTPS 和医疗数据脱敏策略。
5. **医疗能力边界明确**：当前不做药物相互作用判断、不自动调整剂量或时间，也不替代医生决策。

## 8. 下周建议计划

### P0：优先闭合真实提醒链路

- 明确 Medication Service ↔ Chat Agent 的事件协议和 `interaction_id` 传递方式；
- 接入 Device Event Adapter，补齐 TTS 开始、完成、失败和打断回执；
- 验证无活跃会话时的主动播报和设备离线场景；
- 用真实 ASR 文本验证“吃了 / 晚点 / 跳过 / 再说一遍”以及歧义澄清。

### P1：增强语义层和可靠性

- 增加模型超时、限流、重试和低置信度策略；
- 增加真实 Harness 响应分类测试集；
- 评估 Harness session 日志中的医疗文本留存和脱敏；
- 增加故障注入和端到端链路指标。

### P2：面向生产部署

- SQLite 迁移 PostgreSQL；
- SQLite Outbox 替换为 Redis Streams Consumer Group；
- 增加认证、审批授权、老人数据隔离、备份和恢复演练；
- 增加家属查询、未确认升级和通知能力。

## 9. 组会需要讨论的决策

- 下一阶段优先投入真实设备/LiveKit 链路，还是先完成 Redis/PostgreSQL 生产基础设施？
- “晚点”允许的最大延迟次数和确认截止时间如何配置？
- `closed_unconfirmed` 后是否需要通知家属，以及升级策略由谁负责？
- Harness session 中的医疗文本是否留存、留存多久、如何脱敏？

## 10. 相关代码与文档

- 领域服务：[src/medication_reminder/service.py](./src/medication_reminder/service.py)
- 持久化：[src/medication_reminder/storage.py](./src/medication_reminder/storage.py)
- 语义 Agent：[src/medication_reminder/semantic/agent.py](./src/medication_reminder/semantic/agent.py)
- Harness 适配器：[src/medication_reminder/semantic/harness_adapter.py](./src/medication_reminder/semantic/harness_adapter.py)
- HTTP API：[src/medication_reminder/http.py](./src/medication_reminder/http.py)
- Web 页面：[web/index.html](./web/index.html)
- 自动化测试：[tests/test_mvp.py](./tests/test_mvp.py)、[tests/test_semantic_agent.py](./tests/test_semantic_agent.py)
- 项目运行说明：[README.md](./README.md)
- 详细实现进度：[实现进度.md](./实现进度.md)

